JavaScript:
	functions:
		xcrement(player, amount)
		setGradient(player, letters)
		addColor(player, letter)
		resetColors(player)
	elements:
		background
		playerName
		lifeAddOne
		lifeAddFive
		lifeLoseOne
		lifeLoseFive
		lifeTotal
		colorsList
		colorButton
	replacements:
		%thingy%